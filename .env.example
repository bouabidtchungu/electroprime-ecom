# Admin Security
# Change 'admin123' to a strong secret password for your production site
ADMIN_PASSWORD=admin123

# Server Configuration
PORT=3001
NODE_ENV=production

# Database Configuration
# Get your URI from MongoDB Atlas (free tier)
MONGODB_URI=mongodb+srv://<user>:<password>@cluster.mongodb.net/electroprime
